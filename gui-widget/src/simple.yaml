styles:
  - widget: Text
    properties:
      colour: "#ffffff"
      font: arial
      size: 12

components:
  - name: Counter # Names for components must be unique
    child:
      name: Count
      widget: Text
      properties:
        text: Hi # Can use any variable from the component
