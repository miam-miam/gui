styles:
  - widget: Text
    properties:
      colour: "#ffff00"
      size: 24

components:
  - name: Counter # Names for components must be unique
    variables:
      - name: name
        type: String
    child:
      name: Count
      widget: Button
      properties:
        background_colour: "#00ff00"