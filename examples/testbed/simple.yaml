styles:
  - widget: Text
    properties:
      colour: "#ffff00"
      size: 12

components:
  - name: Counter # Names for components must be unique
    variables:
      - name: name
        type: String
    child:
      name: Count
      widget: Text
      properties:
        text: Hiya {$name}
        size: 20
#        colour: "#00ff00"